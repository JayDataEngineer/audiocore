{
  "_comment": "Container-friendly default audiocore server config. Ships with NO models so 'docker run -e AUDIOCORE_ALLOW_EMPTY=1 audiocore' boots cleanly for smoke tests. To actually serve requests, mount real GGUF weights under /models and override this file with a populated server.json (see examples/server.json in the repo).",

  "host":   "0.0.0.0",
  "port":   8080,
  "device": 0,
  "threads": 4,

  "models": []
}
